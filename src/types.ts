export interface ActionConfig {
  tool: string;
  url: string;
  token: string;
  file: string;
  ref: string;
  head_ref: string;
  sha: string;
  repository: string;
  run_id: string;
  action: string;
}
